<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="cofficedo.list">
      <field name="name">cofficedo list</field>
      <field name="model">cofficedo.cofficedo</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="cofficedo.action_window">
      <field name="name">cofficedo window</field>
      <field name="res_model">cofficedo.cofficedo</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="cofficedo.action_server">
      <field name="name">cofficedo server</field>
      <field name="model_id" ref="model_cofficedo_cofficedo"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="cofficedo" id="cofficedo.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="cofficedo.menu_1" parent="cofficedo.menu_root"/>
    <menuitem name="Menu 2" id="cofficedo.menu_2" parent="cofficedo.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="cofficedo.menu_1_list" parent="cofficedo.menu_1"
              action="cofficedo.action_window"/>
    <menuitem name="Server to list" id="cofficedo" parent="cofficedo.menu_2"
              action="cofficedo.action_server"/>
    -->
  </data>
</odoo>